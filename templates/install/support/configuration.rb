# Global configuration options for cucumber-rails
# Cucumber::Rails::Configuration.configure do
  # Set options (e.g. :only or :except) for database cleaner when the hook sets the :truncation strategy
  # set :truncation_options, :except => %w[widgets]
# end
